name: WebGPU 4.7.2 Candidate Build

on:
  push:
    branches: [main]
    paths:
      - "engine/**"
      - "tools/engine/**"
      - "tools/perf/webgpu_browser_smoke.py"
      - "game/export_presets.cfg"
      - "game/tests/perf/webgpu_boot/**"
      - "game/tests/integration/web_playable_smoke.gd"
      - "game/tests/integration/web_playable_smoke.tscn"
      - "networking/spritely/**"
      - "tools/networking/package_spritely_web.sh"
      - ".github/workflows/webgpu-candidate-build.yml"
  pull_request:
    paths:
      - "engine/**"
      - "tools/engine/**"
      - "tools/perf/webgpu_browser_smoke.py"
      - "game/export_presets.cfg"
      - "game/tests/perf/webgpu_boot/**"
      - "game/tests/integration/web_playable_smoke.gd"
      - "game/tests/integration/web_playable_smoke.tscn"
      - "networking/spritely/**"
      - "tools/networking/package_spritely_web.sh"
      - ".github/workflows/webgpu-candidate-build.yml"
  workflow_dispatch:

concurrency:
  group: webgpu-candidate-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  web-template:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      EM_VERSION: "6.0.9"
      SCONS_CACHE: ${{ github.workspace }}/.cache/scons-godot-webgpu
      STARJUNK_CARGO_TARGET_ROOT: ${{ github.workspace }}/.cache/cargo-webgpu
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Verify exact source revision
        env:
          EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"
      - name: Restore WebGPU build caches
        id: webgpu-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: |
            .cache/scons-godot-webgpu
            .cache/cargo-webgpu
          key: webgpu-${{ runner.os }}-toolchain-${{ env.EM_VERSION }}-${{ hashFiles('engine/source-lock.json', 'engine/patches/*.patch', 'tools/engine/apply_starjunk_port_patches.py', 'tools/engine/build_webgpu_rust_deps.sh') }}
          restore-keys: |
            webgpu-${{ runner.os }}-toolchain-${{ env.EM_VERSION }}-
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Set up Emscripten
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: ${{ env.EM_VERSION }}
          no-cache: true
      - name: Verify pinned Emscripten identity
        run: |
          test "$(python3 -c 'import json; print(json.load(open("engine/source-lock.json"))["emscripten"])')" = "$EM_VERSION"
          emcc --version
      - name: Install build dependencies
        run: python3 -m pip install scons websocket-client
      - name: Prepare merged Godot 4.7.2 + WebGPU candidate
        run: tools/engine/prepare_port_candidate.sh build/webgpu-port-candidate
      - name: Build pinned Rust shader dependencies for Emscripten
        run: tools/engine/build_webgpu_rust_deps.sh build/webgpu-port-candidate/godot build/webgpu-rust-deps
      - name: Compile WebGPU web template
        run: |
          scons -C build/webgpu-port-candidate/godot \
            platform=web \
            target=template_release \
            threads=no \
            opengl3=no \
            webgpu=yes \
            webgpu_backend=emdawnwebgpu \
            dlink_enabled=no \
            lto=none \
            debug_symbols=no \
            linkflags="--profiling-funcs" \
            cache_path="$SCONS_CACHE" \
            cache_limit=8 \
            -j2
      - name: Save WebGPU build caches
        uses: actions/cache/save@v4
        with:
          path: |
            .cache/scons-godot-webgpu
            .cache/cargo-webgpu
          key: ${{ steps.webgpu-cache-restore.outputs.cache-primary-key }}
      - name: List build outputs
        if: always()
        run: find build/webgpu-port-candidate/godot/bin -maxdepth 1 -type f -printf '%f %s bytes\n' 2>/dev/null | sort || true
      - uses: actions/upload-artifact@v4
        with:
          name: godot-4.7.2-webgpu-template-debug
          path: build/webgpu-port-candidate/godot/bin/godot.web.template_release.wasm32.nothreads.zip
          if-no-files-found: error
      - name: Download upstream Godot editor for export
        env:
          GODOT_VERSION: "4.7.2"
        run: |
          curl -fL --retry 3 -o /tmp/godot.zip \
            "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          unzip -q /tmp/godot.zip -d /tmp/godot-editor
          chmod +x /tmp/godot-editor/Godot_v${GODOT_VERSION}-stable_linux.x86_64
          echo "GODOT_BIN=/tmp/godot-editor/Godot_v${GODOT_VERSION}-stable_linux.x86_64" >> "$GITHUB_ENV"
      - name: Import game before Web export
        run: '"$GODOT_BIN" --headless --path game --editor --quit-after 2'
      - name: Export lightweight Godot WebGPU boot scene
        run: |
          tools/engine/export_webgpu_benchmark.sh \
            "$GODOT_BIN" \
            "$GITHUB_WORKSPACE/build/webgpu-port-candidate/godot/bin/godot.web.template_release.wasm32.nothreads.zip" \
            "$GITHUB_WORKSPACE/build/webgpu-boot" \
            "res://tests/perf/webgpu_boot/webgpu_boot.tscn"
      - name: Run Chromium Godot WebGPU boot gate
        id: browser-gate
        continue-on-error: true
        run: |
          python3 tools/perf/webgpu_browser_smoke.py \
            --root build/webgpu-boot \
            --output build/webgpu-boot-smoke.json \
            --result-global __STARJUNK_BOOT_RESULT__ \
            --purpose browser_webgpu_boot \
            --expected-profile boot \
            --spirv-dump-dir build/webgpu-spv \
            --timeout 45
          cat build/webgpu-boot-smoke.json
      - name: Analyze captured SPIR-V
        if: always()
        run: |
          if compgen -G 'build/webgpu-spv/*.spv' > /dev/null; then
            sudo apt-get update
            sudo apt-get install -y spirv-tools
            bash tools/engine/analyze_spirv_dumps.sh build/webgpu-spv build/webgpu-spv-analysis
            cat build/webgpu-spv-analysis/summary.txt
          else
            echo "No failing SPIR-V dumps captured."
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: webgpu-browser-boot
          path: build/webgpu-boot-smoke.json
          if-no-files-found: warn
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: webgpu-spv-dumps
          path: build/webgpu-spv/
          if-no-files-found: warn
      - name: Build and validate pinned Spritely Web runtime
        run: networking/spritely/run_spike.sh
      - name: Export real PrototypeRace browser smoke
        run: |
          tools/engine/export_webgpu_playable.sh \
            "$GODOT_BIN" \
            "$GITHUB_WORKSPACE/build/webgpu-port-candidate/godot/bin/godot.web.template_release.wasm32.nothreads.zip" \
            "$GITHUB_WORKSPACE/build/webgpu-playable-smoke" \
            "res://tests/integration/web_playable_smoke.tscn"
          tools/networking/package_spritely_web.sh "$GITHUB_WORKSPACE/build/webgpu-playable-smoke"
          (
            cd "$GITHUB_WORKSPACE/build/webgpu-playable-smoke/spritely"
            sha256sum -c SHA256SUMS
          )
      - name: Run Chromium playable + Spritely gate
        run: |
          python3 tools/perf/webgpu_browser_smoke.py \
            --root build/webgpu-playable-smoke \
            --output build/webgpu-playable-smoke.json \
            --result-global __STARJUNK_PLAYABLE_RESULT__ \
            --purpose browser_webgpu_playable \
            --expected-profile playable \
            --required-console-prefix STARJUNK_SPRITELY_READY_JSON: \
            --timeout 60
          python3 - <<'PY'
          import json
          from pathlib import Path

          result = json.loads(Path("build/webgpu-playable-smoke.json").read_text())
          payload = result.get("payload", {})
          if payload.get("vehicle_ready") is not True:
              raise SystemExit(f"PrototypeRace vehicle was not ready: {payload}")
          if int(payload.get("checkpoint_count", 0)) < 2:
              raise SystemExit(f"PrototypeRace checkpoints were not built: {payload}")
          print(json.dumps(payload, indent=2))
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: webgpu-browser-playable-smoke
          path: build/webgpu-playable-smoke.json
          if-no-files-found: warn
      - name: Export shareable PrototypeRace browser demo
        run: |
          tools/engine/export_webgpu_playable.sh \
            "$GODOT_BIN" \
            "$GITHUB_WORKSPACE/build/webgpu-port-candidate/godot/bin/godot.web.template_release.wasm32.nothreads.zip" \
            "$GITHUB_WORKSPACE/build/web-demo"
          tools/networking/package_spritely_web.sh "$GITHUB_WORKSPACE/build/web-demo"
          (
            cd "$GITHUB_WORKSPACE/build/web-demo/spritely"
            sha256sum -c SHA256SUMS
          )
      - uses: actions/upload-artifact@v4
        with:
          name: starjunk-racer-95-browser-demo
          path: build/web-demo/
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: webgpu-spv-analysis
          path: build/webgpu-spv-analysis/
          if-no-files-found: warn
      - name: Enforce Chromium WebGPU boot gate
        if: ${{ always() && steps.browser-gate.outcome != 'success' }}
        run: exit 1
